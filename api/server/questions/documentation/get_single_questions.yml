User can view single question 
Any user can view a single question via its Id.
    ---
    tags:
      - Create new question Endpoint
    summary: View info
    consumes:
      - application/json
    produces:
      - application/json